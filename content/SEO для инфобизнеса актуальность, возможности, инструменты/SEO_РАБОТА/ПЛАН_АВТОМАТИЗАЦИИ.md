# ПЛАН АВТОМАТИЗАЦИИ АНАЛИЗА КОНКУРЕНТОВ

**Цель:** Автоматизировать процесс анализа конкурентов из урока 3
**Текущая проблема:** Ручной анализ 5 статей занимает 30-40 минут
**Целевое время:** 2-3 минуты (запуск скрипта + проверка результата)

---

## ЧТО ДЕЛАЕТ СКРИПТ

### Входные данные:
```
Ключевой запрос: "почему не худею"
```

### Выходные данные:
```
1. Excel-файл с анализом ТОП-5 конкурентов:
   - Ссылки на статьи
   - Объем каждой статьи (знаков без пробелов)
   - Заголовки
   - Подзаголовки (структура)
   - Средний объем

2. Готовое ТЗ на статью:
   - Основная тема
   - Рекомендуемый объем
   - План статьи (на основе анализа конкурентов)
   - Рекомендуемый заголовок
```

---

## АРХИТЕКТУРА РЕШЕНИЯ

### Вариант 1: Python-скрипт с веб-драйвером 🔥 РЕКОМЕНДУЕТСЯ

**Технологии:**
- Python 3.10+
- Selenium (эмуляция браузера для обхода защиты Яндекса)
- BeautifulSoup (парсинг HTML)
- pandas (работа с таблицами)
- openpyxl (экспорт в Excel)

**Преимущества:**
- ✅ Полная автоматизация
- ✅ Работает с любыми ключами
- ✅ Обходит защиту от ботов
- ✅ Можно расширять функционал

**Недостатки:**
- ⚠️ Требует установки зависимостей
- ⚠️ Может быть медленным (20-30 сек на анализ)

### Вариант 2: Claude Agent через API 🚀 БЫСТРЕЕ

**Технологии:**
- Claude API (Anthropic)
- Python для оркестрации
- MCP (Model Context Protocol) для доступа к веб-страницам

**Преимущества:**
- ✅ Более умный анализ (AI понимает контекст)
- ✅ Может генерировать лучшие заголовки и план
- ✅ Легче в поддержке

**Недостатки:**
- ⚠️ Требует API-ключ Claude ($)
- ⚠️ Зависит от доступности API

### Вариант 3: Гибридный подход ⚡ ОПТИМАЛЬНЫЙ

**Схема:**
1. Python (Selenium) → Получить ТОП-5 URL из Яндекса
2. Python (BeautifulSoup) → Спарсить контент статей
3. Claude Agent → Проанализировать контент и создать ТЗ

**Преимущества:**
- ✅ Надежный сбор данных (Python)
- ✅ Умный анализ (Claude)
- ✅ Лучшее качество результата

---

## ДЕТАЛЬНЫЙ ПЛАН РАЗРАБОТКИ

### ЭТАП 1: Парсинг поисковой выдачи Яндекса

**Задача:** Получить ТОП-10 URL по ключевому запросу

**Файл:** `ИНСТРУМЕНТЫ/скрипты/yandex_parser.py`

**Алгоритм:**
```python
def get_top_urls(keyword: str, count: int = 10) -> list[str]:
    """
    Получает топ-N URL из поисковой выдачи Яндекса

    Args:
        keyword: Ключевой запрос
        count: Количество результатов

    Returns:
        Список URL статей (без рекламы, видео, брендов)
    """

    # 1. Запустить Selenium WebDriver (Chrome)
    driver = webdriver.Chrome()

    # 2. Открыть Яндекс
    driver.get(f"https://yandex.ru/search/?text={keyword}")

    # 3. Дождаться загрузки результатов
    wait.until(EC.presence_of_element_located((By.CSS_SELECTOR, ".serp-item")))

    # 4. Спарсить результаты
    results = driver.find_elements(By.CSS_SELECTOR, ".serp-item")

    urls = []
    for result in results:
        # Пропустить рекламу
        if result.find_elements(By.CSS_SELECTOR, ".label_theme_direct"):
            continue

        # Пропустить видео
        if result.find_elements(By.CSS_SELECTOR, ".serp-item_type_video"):
            continue

        # Получить URL
        link = result.find_element(By.CSS_SELECTOR, "a.link").get_attribute("href")
        urls.append(link)

        if len(urls) >= count:
            break

    driver.quit()
    return urls
```

**Проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Яндекс блокирует ботов | Использовать User-Agent браузера, случайные задержки |
| CAPTCHA | Использовать Selenium Stealth, 2captcha API |
| Динамическая загрузка | Использовать явные ожидания (WebDriverWait) |

### ЭТАП 2: Парсинг контента статей

**Задача:** Извлечь контент, заголовок, подзаголовки из каждой статьи

**Файл:** `ИНСТРУМЕНТЫ/скрипты/content_parser.py`

**Алгоритм:**
```python
def parse_article(url: str) -> dict:
    """
    Парсит контент статьи

    Returns:
        {
            'url': str,
            'title': str,           # H1 заголовок
            'content': str,         # Весь текст
            'char_count': int,      # Символов без пробелов
            'headings': list[str]   # H2, H3 подзаголовки
        }
    """

    # 1. Загрузить HTML
    response = requests.get(url, headers=HEADERS)
    soup = BeautifulSoup(response.text, 'html.parser')

    # 2. Извлечь заголовок (H1)
    h1 = soup.find('h1')
    title = h1.get_text(strip=True) if h1 else "Заголовок не найден"

    # 3. Извлечь подзаголовки (H2, H3)
    headings = []
    for tag in soup.find_all(['h2', 'h3']):
        headings.append(tag.get_text(strip=True))

    # 4. Извлечь весь текст
    # Удалить скрипты, стили, навигацию
    for tag in soup(['script', 'style', 'nav', 'footer', 'header']):
        tag.decompose()

    content = soup.get_text(separator=' ', strip=True)

    # 5. Подсчитать символы без пробелов
    char_count = len(content.replace(' ', ''))

    return {
        'url': url,
        'title': title,
        'content': content,
        'char_count': char_count,
        'headings': headings
    }
```

**Проблемы и решения:**

| Проблема | Решение |
|----------|---------|
| Разная структура сайтов | Универсальный парсинг (все <p>, <h1-h6>) |
| Мусорный контент (реклама, меню) | Удаление тегов nav, footer, aside, .ad |
| Анти-бот защита | requests + User-Agent, или Selenium |

### ЭТАП 3: Анализ конкурентов (Claude Agent)

**Задача:** Проанализировать спарсенные данные и создать ТЗ

**Файл:** `ИНСТРУМЕНТЫ/скрипты/claude_analyzer.py`

**Алгоритм:**
```python
def analyze_competitors(keyword: str, articles: list[dict]) -> dict:
    """
    Анализирует конкурентов с помощью Claude AI

    Args:
        keyword: Ключевой запрос
        articles: Список спарсенных статей

    Returns:
        {
            'avg_length': int,
            'recommended_title': str,
            'article_plan': list[str],
            'key_topics': list[str],
            'marketing_idea': str
        }
    """

    # 1. Подготовить промпт для Claude
    prompt = f"""
    Ты - эксперт по SEO-анализу конкурентов.

    ЗАДАЧА: Проанализируй ТОП-5 статей по запросу "{keyword}" и создай ТЗ.

    ДАННЫЕ О КОНКУРЕНТАХ:
    {format_articles_data(articles)}

    ТВОИ ДЕЙСТВИЯ:
    1. Вычисли средний объем статей
    2. Найди общие микротемы (подзаголовки, которые повторяются у 3+ конкурентов)
    3. Составь оптимальный план статьи (8-12 подзаголовков)
    4. Предложи 3 варианта заголовка (с ключевым словом и цифрами)
    5. Определи маркетинговую идею для партнерского арбитража

    ФОРМАТ ОТВЕТА: JSON
    """

    # 2. Отправить запрос в Claude API
    response = anthropic.messages.create(
        model="claude-sonnet-4-5-20250929",
        max_tokens=4000,
        messages=[{"role": "user", "content": prompt}]
    )

    # 3. Распарсить ответ
    result = json.loads(response.content[0].text)

    return result
```

### ЭТАП 4: Экспорт в Excel

**Задача:** Создать файл Excel с результатами анализа

**Файл:** `ИНСТРУМЕНТЫ/скрипты/excel_exporter.py`

**Алгоритм:**
```python
def export_to_excel(keyword: str, articles: list[dict], analysis: dict) -> str:
    """
    Экспортирует результаты анализа в Excel

    Returns:
        Путь к созданному файлу
    """

    # 1. Создать DataFrame для конкурентов
    df_competitors = pd.DataFrame([
        {
            'Ссылка': article['url'],
            'Объем (знаков)': article['char_count'],
            'Заголовок': article['title'],
            **{f'Подзаголовок {i+1}': h
               for i, h in enumerate(article['headings'][:7])}
        }
        for article in articles[:5]
    ])

    # 2. Создать DataFrame для ТЗ
    df_tz = pd.DataFrame([
        {'Параметр': 'Средний объем', 'Значение': analysis['avg_length']},
        {'Параметр': 'Основная тема', 'Значение': keyword},
        {'Параметр': 'План статьи', 'Значение': '\n'.join(analysis['article_plan'])},
        {'Параметр': 'Рекомендуемый заголовок', 'Значение': analysis['recommended_title']},
        {'Параметр': 'Маркетинговая идея', 'Значение': analysis['marketing_idea']}
    ])

    # 3. Сохранить в Excel
    filename = f"01_АНАЛИЗ_КОНКУРЕНТОВ/ключ_{keyword.replace(' ', '_')}/анализ_{datetime.now().strftime('%Y%m%d_%H%M%S')}.xlsx"

    with pd.ExcelWriter(filename, engine='openpyxl') as writer:
        df_competitors.to_excel(writer, sheet_name='Конкуренты', index=False)
        df_tz.to_excel(writer, sheet_name='ТЗ', index=False)

    return filename
```

### ЭТАП 5: Главный скрипт-оркестратор

**Задача:** Объединить все модули в единый процесс

**Файл:** `ИНСТРУМЕНТЫ/скрипты/main.py`

**Алгоритм:**
```python
def analyze_keyword(keyword: str, use_ai: bool = True):
    """
    Полный цикл анализа конкурентов

    Args:
        keyword: Ключевой запрос
        use_ai: Использовать ли Claude для анализа
    """

    print(f"🔍 Анализирую запрос: '{keyword}'")

    # 1. Получить URL конкурентов из Яндекса
    print("⏳ Получаю ТОП-10 из Яндекса...")
    urls = yandex_parser.get_top_urls(keyword, count=10)
    print(f"✅ Найдено {len(urls)} статей")

    # 2. Спарсить контент статей
    print("⏳ Парсю контент статей...")
    articles = []
    for i, url in enumerate(urls[:5], 1):
        print(f"  [{i}/5] {url}")
        try:
            article = content_parser.parse_article(url)
            articles.append(article)
        except Exception as e:
            print(f"  ⚠️ Ошибка: {e}")

    print(f"✅ Спарсено {len(articles)} статей")

    # 3. Анализ с помощью Claude (опционально)
    if use_ai:
        print("⏳ Запускаю AI-анализ через Claude...")
        analysis = claude_analyzer.analyze_competitors(keyword, articles)
        print("✅ Анализ завершен")
    else:
        # Простой анализ без AI
        analysis = simple_analyzer.analyze(articles)

    # 4. Экспорт в Excel
    print("⏳ Создаю Excel-файл...")
    filename = excel_exporter.export_to_excel(keyword, articles, analysis)
    print(f"✅ Готово! Файл: {filename}")

    # 5. Показать краткие результаты
    print("\n" + "="*60)
    print(f"📊 РЕЗУЛЬТАТЫ АНАЛИЗА ПО ЗАПРОСУ '{keyword}'")
    print("="*60)
    print(f"Средний объем статей: {analysis['avg_length']} знаков")
    print(f"Рекомендуемый заголовок: {analysis['recommended_title']}")
    print(f"\nПлан статьи:")
    for i, topic in enumerate(analysis['article_plan'], 1):
        print(f"  {i}. {topic}")
    print("="*60)

    return filename


# Точка входа
if __name__ == "__main__":
    import sys

    if len(sys.argv) < 2:
        print("Использование: python main.py 'ключевой запрос'")
        sys.exit(1)

    keyword = sys.argv[1]
    analyze_keyword(keyword, use_ai=True)
```

---

## ИСПОЛЬЗОВАНИЕ СКРИПТА

### Установка зависимостей

```bash
# Перейти в папку проекта
cd "/Volumes/T7 Shield/SEO для инфобизнеса актуальность, возможности, инструменты/SEO_РАБОТА"

# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt
```

**Файл:** `requirements.txt`
```
selenium==4.16.0
beautifulsoup4==4.12.3
pandas==2.2.0
openpyxl==3.1.2
anthropic==0.18.0  # Если используется Claude API
requests==2.31.0
webdriver-manager==4.0.1  # Автоустановка драйвера Chrome
```

### Запуск анализа

**Вариант 1: Через командную строку**
```bash
cd ИНСТРУМЕНТЫ/скрипты
python main.py "почему не худею"
```

**Вариант 2: Через Python**
```python
from ИНСТРУМЕНТЫ.скрипты import main

# Анализ одного ключа
main.analyze_keyword("почему не худею")

# Анализ списка ключей
keywords = [
    "почему не худею",
    "питание и дефицит",
    "как нормализовать вес"
]

for keyword in keywords:
    main.analyze_keyword(keyword)
```

### Результат работы

После запуска будет создан файл:
```
01_АНАЛИЗ_КОНКУРЕНТОВ/ключ_почему_не_худею/анализ_20260122_150045.xlsx
```

**Содержимое файла:**

**Лист 1: Конкуренты**
| Ссылка | Объем | Заголовок | Подзаголовок 1 | Подзаголовок 2 | ... |
|--------|-------|-----------|----------------|----------------|-----|
| https://... | 8500 | Почему я не худею... | Метаболизм | Дефициты | ... |

**Лист 2: ТЗ**
| Параметр | Значение |
|----------|----------|
| Средний объем | 10300 знаков |
| Основная тема | Почему не худею |
| План статьи | 1. Введение...<br>2. Метаболизм... |
| Рекомендуемый заголовок | Почему я не худею: 7 скрытых причин |

---

## РАСШИРЕННЫЕ ВОЗМОЖНОСТИ

### Функция 1: Пакетный анализ

**Задача:** Проанализировать список ключей за раз

**Файл:** `ИНСТРУМЕНТЫ/скрипты/batch_analyzer.py`

```python
def batch_analyze(keywords: list[str]):
    """Пакетный анализ списка ключевых запросов"""

    results = []

    for i, keyword in enumerate(keywords, 1):
        print(f"\n{'='*60}")
        print(f"[{i}/{len(keywords)}] Анализирую: {keyword}")
        print('='*60)

        try:
            filename = main.analyze_keyword(keyword)
            results.append({
                'keyword': keyword,
                'status': 'success',
                'file': filename
            })
        except Exception as e:
            results.append({
                'keyword': keyword,
                'status': 'error',
                'error': str(e)
            })

    # Сохранить отчет
    report_df = pd.DataFrame(results)
    report_df.to_excel('05_АНАЛИТИКА/batch_report.xlsx', index=False)

    print("\n✅ Пакетный анализ завершен!")
    print(f"Обработано: {len(keywords)} запросов")
    print(f"Успешно: {len([r for r in results if r['status'] == 'success'])}")
```

### Функция 2: Мониторинг конкурентов

**Задача:** Отслеживать изменения в статьях конкурентов

**Файл:** `ИНСТРУМЕНТЫ/скрипты/competitor_monitor.py`

```python
def monitor_competitors(keyword: str):
    """
    Мониторинг конкурентов (запускать раз в неделю)

    Сравнивает текущий топ с предыдущим анализом:
    - Изменились ли позиции?
    - Обновились ли статьи (объем, структура)?
    - Появились ли новые конкуренты?
    """

    # 1. Загрузить предыдущий анализ
    previous = load_previous_analysis(keyword)

    # 2. Сделать новый анализ
    current = analyze_keyword(keyword)

    # 3. Сравнить
    changes = compare_analyses(previous, current)

    # 4. Уведомить, если есть критичные изменения
    if changes['new_competitors'] or changes['position_changes']:
        send_notification(f"⚠️ Изменения в топе по '{keyword}'")

    return changes
```

### Функция 3: Автоматическое создание статьи

**Задача:** Сразу после анализа создать черновик статьи

**Файл:** `ИНСТРУМЕНТЫ/скрипты/article_generator.py`

```python
def generate_article(keyword: str, analysis: dict) -> str:
    """
    Генерирует статью на основе анализа конкурентов

    Returns:
        Путь к созданной статье
    """

    prompt = f"""
    Создай SEO-статью на основе анализа конкурентов.

    КЛЮЧЕВОЙ ЗАПРОС: {keyword}
    ЗАГОЛОВОК: {analysis['recommended_title']}
    ОБЪЕМ: {analysis['avg_length']} знаков
    ПЛАН СТАТЬИ:
    {format_plan(analysis['article_plan'])}

    ТРЕБОВАНИЯ:
    - Экспертный, но понятный тон
    - Примеры из практики
    - 2 партнерские ссылки (после 3-го и последнего подзаголовка)
    - Маркетинговая идея: {analysis['marketing_idea']}
    """

    # Генерация через Claude
    article = claude.generate(prompt)

    # Сохранить
    filename = f"03_ГОТОВЫЕ_СТАТЬИ/{keyword.replace(' ', '_')}_{datetime.now().strftime('%Y%m%d')}.md"
    with open(filename, 'w', encoding='utf-8') as f:
        f.write(article)

    return filename
```

---

## ROADMAP РАЗРАБОТКИ

### ФАЗА 1: MVP (Минимально работающий продукт) ⏱️ 2-3 дня

**Задачи:**
- [x] Установить зависимости
- [ ] Разработать `yandex_parser.py` (парсинг Яндекса)
- [ ] Разработать `content_parser.py` (парсинг статей)
- [ ] Разработать `simple_analyzer.py` (базовый анализ без AI)
- [ ] Разработать `excel_exporter.py` (экспорт в Excel)
- [ ] Разработать `main.py` (оркестратор)
- [ ] Протестировать на ключе "почему не худею"

**Результат:** Базовый скрипт, который экономит 80% времени

### ФАЗА 2: AI-улучшения ⏱️ 1-2 дня

**Задачи:**
- [ ] Интегрировать Claude API
- [ ] Разработать `claude_analyzer.py`
- [ ] Улучшить качество генерации плана статьи
- [ ] Добавить генерацию заголовков

**Результат:** Более качественный анализ и ТЗ

### ФАЗА 3: Автоматизация создания статей ⏱️ 2-3 дня

**Задачи:**
- [ ] Разработать `article_generator.py`
- [ ] Интегрировать проверку уникальности (text.ru API)
- [ ] Интегрировать проверку Тургенев
- [ ] Автоматическая вставка партнерских ссылок

**Результат:** Полный цикл от ключа до готовой статьи

### ФАЗА 4: Публикация и аналитика ⏱️ 3-5 дней

**Задачи:**
- [ ] Интеграция с Яндекс.Дзен API
- [ ] Интеграция с ВКонтакте API
- [ ] Автопубликация статей
- [ ] Dashboard аналитики (позиции, клики, продажи)

**Результат:** Полностью автоматизированная система

---

## АЛЬТЕРНАТИВНЫЕ ПОДХОДЫ

### Подход 1: No-Code решение (Zapier/n8n)

**Описание:**
- Использовать готовые сервисы для автоматизации
- n8n: бесплатный аналог Zapier (self-hosted)

**Workflow:**
```
1. Google Sheets (ввод ключа)
   →
2. HTTP Request (парсинг Яндекса через API)
   →
3. Loop (для каждого URL)
   →
4. HTTP Request (получение контента)
   →
5. Claude AI (анализ)
   →
6. Google Sheets (вывод результата)
```

**Преимущества:**
- ✅ Не нужно программировать
- ✅ Визуальный интерфейс
- ✅ Легко менять логику

**Недостатки:**
- ⚠️ Меньше гибкости
- ⚠️ Зависимость от сторонних сервисов

### Подход 2: Расширение браузера

**Описание:**
- Chrome Extension, которое работает прямо в Яндексе

**Как работает:**
1. Открываешь Яндекс
2. Вбиваешь ключ
3. Нажимаешь кнопку расширения
4. Расширение парсит топ-5 прямо в браузере
5. Показывает результат в popup

**Преимущества:**
- ✅ Удобно для ручной работы
- ✅ Не нужен сервер

**Недостатки:**
- ⚠️ Требует знания JavaScript
- ⚠️ Нельзя автоматизировать полностью

---

## СТОИМОСТЬ И РЕСУРСЫ

### Бесплатный вариант (Фаза 1)

**Что нужно:**
- Python (бесплатно)
- Selenium (бесплатно)
- Время на разработку: 2-3 дня

**Ограничения:**
- Базовый анализ без AI
- Ручная генерация статей

### Платный вариант (Фазы 2-4)

**Дополнительные затраты:**
- Claude API: $20-50/месяц (в зависимости от объема)
- text.ru API: ~500 руб/месяц (100 проверок)
- Тургенев: 15 руб/проверка (~1000 руб/месяц на 70 статей)

**ИТОГО:** ~$30-40/месяц (~3000 руб)

**ROI (Return on Investment):**
- Экономия времени: 30 часов/месяц × 1000 руб/час = 30 000 руб
- Дополнительный доход: 5-10 статей больше = +30 000 - 60 000 руб/месяц
- **Окупаемость: 1 день**

---

## СЛЕДУЮЩИЕ ШАГИ

### Что нужно решить СЕЙЧАС:

1. **Выбрать подход к автоматизации:**
   - [ ] Python-скрипт (рекомендуется)
   - [ ] No-Code (n8n)
   - [ ] Расширение браузера

2. **Определить бюджет:**
   - [ ] Бесплатно (без AI)
   - [ ] $20-40/месяц (с AI)

3. **Определить приоритет:**
   - [ ] Быстрый MVP (Фаза 1) - начать зарабатывать через 3 дня
   - [ ] Полная автоматизация (Фазы 1-4) - начать через 2 недели

### Мои рекомендации:

🎯 **ОПТИМАЛЬНЫЙ ПЛАН:**

**Неделя 1:** Разработать MVP (Фаза 1)
- Ты сможешь анализировать конкурентов за 2 минуты
- Создавать статьи вручную (с помощью ChatGPT)
- Опубликовать первые 3-5 статей

**Неделя 2:** Добавить AI (Фаза 2)
- Улучшить качество анализа
- Автоматизировать генерацию заголовков и планов

**Неделя 3-4:** Автоматизировать создание статей (Фаза 3)
- Полный цикл: ключ → готовая статья за 5 минут

**Месяц 2-3:** Масштабирование (Фаза 4)
- Автопубликация
- Аналитика
- 30-50 статей в месяц на автомате

---

**ГОТОВ НАЧАТЬ РАЗРАБОТКУ ПРИ ТВОЕМ ОДОБРЕНИИ! 🚀**

Какой подход тебе ближе? Или есть вопросы по плану?
