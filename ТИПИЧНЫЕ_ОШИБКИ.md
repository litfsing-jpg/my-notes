# ⚠️ ТИПИЧНЫЕ ОШИБКИ И ИХ РЕШЕНИЯ

Частые проблемы при работе с блогом и их решения.

---

## 🔴 ПРОБЛЕМЫ С КНОПКАМИ

### ❌ Кнопка показывается как код (HTML текст)
**Причина:** HTML блокируется в Quartz

**Решение:** Используй markdown ссылку в отдельном параграфе
```markdown
[ТЕКСТ КНОПКИ](https://url.com)
```
Параграф должен содержать ТОЛЬКО ссылку!

---

### ❌ Кнопка не центрируется на мобильных
**Причина:** Старая версия CSS

**Решение:** Обнови код, должно быть исправлено в commit `46830fc`

---

### ❌ В кнопке виден квадратик со стрелочкой
**Причина:** Иконка внешней ссылки

**Решение:** Обнови CSS, исправлено в commit `f3d9398`

---

## 📸 ПРОБЛЕМЫ С ИЗОБРАЖЕНИЯМИ

### ❌ Изображения не отображаются на сайте
**Причина 1:** Неправильный путь

**Решение:**
```markdown
✅ Правильно:
![Описание](/images/articles/photo.webp)

❌ Неправильно:
![[photo.png]]  ← Obsidian-синтаксис
![Описание](../images/photo.webp)  ← Относительный путь
```

**Причина 2:** Файл не добавлен в git

**Решение:**
```bash
git add content/images/articles/photo.webp
git commit -m "Add image"
git push
```

---

### ❌ Изображения слишком большие/маленькие
**Причина:** Не оптимизированы

**Решение:** Используй https://squoosh.app/
- Ширина: 700px для статей, 800px для обложек
- Формат: WebP
- Качество: 70-75
- Вес: < 100KB

---

### ❌ Фото в карточках не на всю ширину
**Причина:** Старая версия CSS

**Решение:** Обнови код, исправлено в commit `f3d9398`

---

## 📑 ПРОБЛЕМЫ С ОГЛАВЛЕНИЕМ

### ❌ Оглавление не сворачивается
**Причина:** Отсутствуют CSS стили для .collapsed

**Решение:** Обнови CSS, исправлено в commit `58880eb`

---

### ❌ Текст в оглавлении плохо читается
**Причина:** Низкая непрозрачность (opacity: 0.35)

**Решение:** Обнови CSS, исправлено в commit `27357fa`

---

### ❌ Показываются все заголовки (H1, H2, H3)
**Причина:** Не настроен фильтр

**Решение:** Обнови CSS, теперь показываются только H2 (commit `58880eb`)

---

## 🚀 ПРОБЛЕМЫ С ДЕПЛОЕМ

### ❌ Сайт не обновляется после git push
**Причина 1:** GitHub Actions не запустился

**Решение:** Проверь вкладку Actions на GitHub

**Причина 2:** Кэш браузера

**Решение:** Ctrl + F5 (жесткая перезагрузка) или Ctrl + Shift + R

---

### ❌ Ошибка при `npx quartz build`
**Причина:** Отсутствует файл или неправильный путь

**Решение:** Проверь:
1. Все изображения существуют в указанных путях
2. Нет ![[obsidian-синтаксиса]] в статьях
3. Все ссылки корректны

---

## 📝 ПРОБЛЕМЫ СО СТАТЬЯМИ

### ❌ Статья не появляется на главной
**Причина 1:** Нет frontmatter

**Решение:** Добавь в начало файла:
```markdown
---
title: "Название"
description: "Описание"
---
```

**Причина 2:** Файл в .gitignore

**Решение:** Проверь, что файл не игнорируется

---

### ❌ Заголовок "Здоровье и Нутрициология" показывается в статье
**Причина:** Старая версия quartz.layout.ts

**Решение:** Обнови layout, исправлено в commit `f3d9398`

---

### ❌ Кириллица в URL статьи
**Причина:** Не указан slug в frontmatter

**Решение:**
```markdown
---
title: "Почему не худею"
slug: "pochemu-ne-khudeyu"  ← Добавь это!
---
```

---

## 🔧 ТЕХНИЧЕСКИЕ ПРОБЛЕМЫ

### ❌ Сервер не запускается (`npx quartz build --serve`)
**Причина 1:** Порт занят

**Решение:**
```bash
# Windows
netstat -ano | findstr :8080
taskkill /PID [номер] /F
```

**Причина 2:** Node.js старая версия

**Решение:** Обнови Node.js до версии 18+

---

### ❌ Git конфликты при push
**Причина:** Локальная версия отстает от remote

**Решение:**
```bash
git pull origin v4
# Разреши конфликты
git add .
git commit -m "Merge conflicts"
git push origin v4
```

---

## 🎨 ПРОБЛЕМЫ СО СТИЛЯМИ

### ❌ Изменения CSS не применяются
**Причина 1:** Кэш

**Решение:** Ctrl + F5 в браузере

**Причина 2:** Не пересобрал сайт

**Решение:**
```bash
rm -rf public
npx quartz build
```

---

### ❌ Стили ломаются на мобильных
**Причина:** Не добавлены media queries

**Решение:** Проверь custom.scss, должны быть `@media (max-width: 768px)`

---

## 📱 ПРОБЛЕМЫ НА МОБИЛЬНЫХ

### ❌ Сайт не адаптирован для мобильных
**Причина:** Старая версия CSS

**Решение:** Обнови custom.scss, все должно быть адаптивно

---

### ❌ Кнопки слишком маленькие на мобильных
**Причина:** Нет адаптивных стилей

**Решение:** Обнови CSS, исправлено в commit `46830fc`

---

## 🔍 КАК НАЙТИ ПРОБЛЕМУ

### Шаг 1: Проверь консоль браузера
```
F12 → Console → ищи красные ошибки
```

### Шаг 2: Проверь сборку
```bash
npx quartz build
# Смотри на ошибки в терминале
```

### Шаг 3: Проверь GitHub Actions
```
GitHub → вкладка Actions → последний workflow
```

### Шаг 4: Проверь коммиты
```bash
git log --oneline -10
# Откатись к рабочей версии если нужно
```

---

## 🆘 БЫСТРЫЕ РЕШЕНИЯ

### Сайт совсем сломался?
```bash
# Откат к последней рабочей версии
git log --oneline
git reset --hard [хеш_коммита]
git push --force origin v4
```

### Локальный сервер не работает?
```bash
# Чистая пересборка
rm -rf public .quartz-cache
npx quartz build --serve
```

### Что-то пропало после обновления?
```bash
# Проверь что добавил в git
git status
git add [файл]
git commit -m "Add missing file"
git push
```

---

## 📞 ЧТО ДЕЛАТЬ ЕСЛИ НИЧЕГО НЕ ПОМОГЛО

1. Проверь `docs/ERRORS_AND_SOLUTIONS.md` - технические ошибки Quartz
2. Проверь `SUMMARY_ДЛЯ_TELEGRAM.md` - полная история изменений
3. Откати к последнему рабочему коммиту
4. Начни с чистого билда

---

**Последнее обновление:** 2026-02-08
**Версия:** 1.0
