# Руководство по работе с изображениями

Подробная инструкция по добавлению и оптимизации изображений для статей блога.

---

## 🖼️ Способы добавления изображений

### Способ 1: Внешние URL (Unsplash, Pexels)

**Преимущества:**
- ✅ Быстрая настройка
- ✅ Не занимают место в репозитории
- ✅ CDN Unsplash обеспечивает быструю загрузку

**Недостатки:**
- ❌ Зависимость от внешнего сервиса
- ❌ Могут блокироваться на некоторых мобильных сетях
- ❌ Проблемы с кэшированием

**Использование:**
```yaml
---
title: "Название статьи"
image: "https://images.unsplash.com/photo-XXXXX?w=800&q=80"
---
```

**Рекомендуемые параметры для Unsplash:**
- `w=800` - ширина 800px
- `q=80` - качество 80%
- `fit=crop` - обрезка по центру

**Примеры источников:**
- [Unsplash](https://unsplash.com/) - бесплатные фото высокого качества
- [Pexels](https://www.pexels.com/) - бесплатные стоковые фото
- [Pixabay](https://pixabay.com/) - бесплатная библиотека изображений

---

### Способ 2: Локальные изображения (рекомендуется)

**Преимущества:**
- ✅ Полный контроль над изображениями
- ✅ Работают оффлайн
- ✅ Нет зависимости от внешних сервисов
- ✅ Лучшее кэширование

**Недостатки:**
- ❌ Увеличивают размер репозитория
- ❌ Нужна оптимизация перед загрузкой

**Структура:**
```
content/
├── images/
│   ├── covers/
│   │   ├── health-1.jpg
│   │   ├── nutrition-2.jpg
│   │   └── ...
│   └── articles/
│       ├── article-1/
│       │   ├── image-1.jpg
│       │   └── image-2.jpg
│       └── ...
└── articles/
    ├── article-1.md
    └── ...
```

**Использование:**
```yaml
---
title: "Название статьи"
image: "/images/covers/health-1.jpg"
# или относительный путь:
# image: "../images/covers/health-1.jpg"
---
```

---

## 🔧 Исправление проблемы "не открываются фото на мобильных"

### Причина проблемы:

1. **Блокировка внешних ресурсов**
   - Некоторые мобильные сети блокируют изображения с внешних доменов
   - Проблемы с CORS или CSP

2. **Медленная загрузка**
   - На мобильных сетях изображения с Unsplash могут загружаться медленно
   - Таймауты при плохом соединении

3. **Отсутствие обработки ошибок**
   - Если изображение не загрузилось, показывается пустое место

### Решения:

#### ✅ Решение 1: Обработка ошибок загрузки (ПРИМЕНЕНО)

Добавлен обработчик `onError` в `RecentNotes.tsx`:
```tsx
<img
  src={image}
  alt={title}
  loading="lazy"
  onError={(e) => {
    const target = e.target as HTMLImageElement
    const container = target.parentElement
    if (container) container.style.display = 'none'
  }}
/>
```

**Что делает:**
- Скрывает контейнер изображения, если оно не загрузилось
- Статья отображается корректно даже без изображения

---

#### ✅ Решение 2: Мобильная оптимизация CSS (ПРИМЕНЕНО)

Добавлены стили для мобильных устройств:
```scss
@media (max-width: 768px) {
  .blog-card-image {
    height: 180px;

    img {
      image-rendering: auto;
      image-rendering: -webkit-optimize-contrast;
    }
  }
}
```

---

#### 🎯 Решение 3: Переход на локальные изображения (РЕКОМЕНДУЕТСЯ)

**Шаг 1: Создайте структуру папок**
```bash
mkdir -p content/images/covers
```

**Шаг 2: Скачайте и оптимизируйте изображения**

Используйте онлайн инструменты:
- [TinyPNG](https://tinypng.com/) - сжатие PNG/JPG
- [Squoosh](https://squoosh.app/) - конвертация в WebP
- [ImageOptim](https://imageoptim.com/) - оптимизация для Mac

**Рекомендуемые параметры:**
- Формат: WebP (или JPG для совместимости)
- Размер: 800x450px (соотношение 16:9)
- Качество: 75-85%
- Вес: до 100 KB

**Шаг 3: Разместите изображения**
```
content/
└── images/
    └── covers/
        ├── pravilnoe-usvoenie-pishchi.jpg
        ├── suhaya-kozha-ruk.jpg
        └── yazva-kishki.jpg
```

**Шаг 4: Обновите frontmatter в статьях**
```yaml
---
title: "Про правильное усвоение пищи"
image: "/images/covers/pravilnoe-usvoenie-pishchi.jpg"
---
```

---

#### 🔄 Решение 4: Fallback изображение

Создайте дефолтное изображение для статей без обложки:

**Вариант A: Градиентный placeholder**

Создайте `content/images/default-cover.svg`:
```svg
<svg width="800" height="450" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#53c257;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#2f9e35;stop-opacity:1" />
    </linearGradient>
  </defs>
  <rect width="800" height="450" fill="url(#grad)" />
  <text x="50%" y="50%" text-anchor="middle" fill="white"
        font-family="Arial" font-size="32" font-weight="bold">
    Здоровье и Питание
  </text>
</svg>
```

**Вариант B: Модификация компонента**

Обновите `RecentNotes.tsx`:
```tsx
const image = page.frontmatter?.image ||
              page.frontmatter?.cover ||
              '/images/default-cover.svg'
```

---

## 📱 Тестирование на мобильных

### Инструменты для проверки:

1. **Chrome DevTools**
   - F12 → Toggle device toolbar
   - Выберите мобильное устройство
   - Проверьте Network → Images

2. **Real Device Testing**
   - Откройте `http://your-ip:8080` на мобильном в той же сети
   - Проверьте загрузку изображений

3. **Lighthouse (Mobile)**
   - F12 → Lighthouse → Mobile
   - Проверьте Performance и Best Practices

### Checklist проверки:

- [ ] Изображения загружаются на мобильных (4G/5G)
- [ ] Изображения загружаются на медленном соединении (3G)
- [ ] Нет пустых мест при ошибках загрузки
- [ ] Карточки выглядят хорошо без изображений
- [ ] Lazy loading работает корректно
- [ ] Изображения не искажаются (object-fit: cover)

---

## ⚡ Оптимизация производительности

### 1. Lazy Loading

Уже реализовано через атрибут `loading="lazy"`:
```tsx
<img src={image} alt={title} loading="lazy" />
```

### 2. Адаптивные изображения (будущее улучшение)

```tsx
<img
  src={image}
  srcSet={`
    ${image}?w=400 400w,
    ${image}?w=800 800w,
    ${image}?w=1200 1200w
  `}
  sizes="(max-width: 768px) 100vw, 400px"
  alt={title}
  loading="lazy"
/>
```

### 3. WebP формат с fallback

```tsx
<picture>
  <source srcSet={image.replace('.jpg', '.webp')} type="image/webp" />
  <img src={image} alt={title} loading="lazy" />
</picture>
```

---

## 🎨 Best Practices

### Рекомендуемые размеры:

- **Cover изображения:** 800x450px (16:9)
- **In-article изображения:** 1200x675px (16:9)
- **Thumbnails:** 400x225px (16:9)

### Форматы:

1. **WebP** - лучшее сжатие, современные браузеры
2. **JPG** - универсальный, для фотографий
3. **PNG** - для изображений с прозрачностью
4. **SVG** - для иконок и простой графики

### Оптимизация:

```bash
# Конвертация в WebP (если установлен cwebp)
cwebp -q 80 input.jpg -o output.webp

# Оптимизация JPG
jpegoptim --max=85 --strip-all image.jpg

# Оптимизация PNG
optipng -o5 image.png
```

---

## 🚨 Troubleshooting

### Проблема: Изображения не отображаются вообще

**Решения:**
1. Проверьте пути к изображениям (абсолютные vs относительные)
2. Убедитесь, что изображения находятся в `content/` или `public/`
3. Проверьте консоль браузера на 404 ошибки

### Проблема: Изображения искажены

**Решение:**
```scss
.blog-card-image img {
  object-fit: cover; // вместо contain или fill
  object-position: center;
}
```

### Проблема: Медленная загрузка

**Решения:**
1. Уменьшите размер изображений (< 100KB)
2. Используйте WebP формат
3. Включите CDN (Cloudflare)
4. Проверьте качество сжатия (q=75-85)

### Проблема: Изображения с Unsplash блокируются

**Решение:**
Перейдите на локальные изображения (см. Решение 3 выше)

---

## 📋 Checklist миграции на локальные изображения

- [ ] Создать папку `content/images/covers/`
- [ ] Скачать изображения для всех статей
- [ ] Оптимизировать изображения (< 100KB)
- [ ] Конвертировать в WebP (опционально)
- [ ] Обновить frontmatter во всех статьях
- [ ] Создать fallback изображение
- [ ] Протестировать на мобильных
- [ ] Проверить производительность (Lighthouse)
- [ ] Закоммитить изменения

---

*Последнее обновление: 2026-02-08*
